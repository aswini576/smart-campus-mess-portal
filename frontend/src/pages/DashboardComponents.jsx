import { Box, Chip, Stack, Typography } from '@mui/material';
import RestaurantRoundedIcon from '@mui/icons-material/RestaurantRounded';
import AppCard from '../components/AppCard';

export function PageHeading({ title, subtitle, action }) { return <Stack direction={{ xs: 'column', sm: 'row' }} justifyContent="space-between" alignItems={{ sm: 'center' }} spacing={2} sx={{ marginBottom: { xs: '32px', sm: '40px' } }}><Box><Typography variant="h4" fontWeight={900}>{title}</Typography><Typography color="text.secondary" fontWeight={600}>{subtitle}</Typography></Box>{action}</Stack>; }
export function MetricCard({ title, value, note, color = '#ff5722' }) { return <AppCard sx={{ height: '100%' }}><Stack direction="row" justifyContent="space-between"><Box><Typography variant="body2" color="text.secondary" fontWeight={650}>{title}</Typography><Typography variant="h4" fontWeight={850} mt={.5}>{value}</Typography></Box><Box sx={{ height: 44, width: 44, borderRadius: 2, bgcolor: `${color}18`, color, display: 'grid', placeItems: 'center' }}><RestaurantRoundedIcon /></Box></Stack><Typography variant="caption" color="text.secondary" display="block" mt={2}>{note}</Typography></AppCard>; }
export function MealChip({ label, status = 'Open' }) { return <Chip label={`${label} · ${status}`} size="small" sx={{ bgcolor: status === 'Closed' ? '#ffebee' : '#fff0e9', color: status === 'Closed' ? '#c62828' : '#d84315', fontWeight: 700 }} />; }
