import { useEffect, useState } from 'react'; import { Alert, Box, Grid, Stack, Typography } from '@mui/material';
import AppButton from '../components/AppButton'; import AppCard from '../components/AppCard'; import ErrorState from '../components/ErrorState'; import LoadingState from '../components/LoadingState'; import { PageHeading } from './DashboardComponents'; import { claimMeal, getAvailableMeals, getClaimedMeals, getOfferedMeals } from '../services/studentService';
const mealLabel = (offer) => { const meal = offer.orderId?.mealId; return meal ? `${meal.mealType} — ${meal.foodName}` : offer.mealType; };
function MealRecoveryPage() { const [available, setAvailable] = useState([]); const [offered, setOffered] = useState([]); const [claimed, setClaimed] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [notice, setNotice] = useState(''); const load = async () => { setLoading(true); try { const [a, o, c] = await Promise.all([getAvailableMeals(), getOfferedMeals(), getClaimedMeals()]); setAvailable(a.offeredMeals); setOffered(o.offeredMeals); setClaimed(c.claimedMeals); } catch (e) { setError(e.response?.data?.message || 'Unable to load meal recovery options.'); } finally { setLoading(false); } }; useEffect(() => { load(); }, []); const claim = async (id) => { setError(''); setNotice(''); try { await claimMeal(id); setNotice('Meal claimed. Your booking has been transferred.'); load(); } catch (e) { setError(e.response?.data?.message || 'Unable to claim meal.'); } }; if (loading) return <LoadingState message="Loading meal recovery…" />; return <><PageHeading title="Meal recovery" subtitle="Offer a confirmed meal or claim an available meal before its expiry time." />{error && <Box mb={2}><ErrorState message={error} /></Box>}{notice && <Alert severity="success" sx={{ mb: 2 }}>{notice}</Alert>}<Grid container spacing={2.5}><Grid size={{ xs: 12, lg: 6 }}><AppCard><Typography variant="h6" fontWeight={800} mb={2}>Available meals</Typography><Stack spacing={1.5}>{available.length ? available.map((offer) => <Box key={offer._id} sx={{ p: 1.5, border: '1px solid #fff0ea', borderRadius: 2 }}><Typography fontWeight={750}>{mealLabel(offer)}</Typography><Typography variant="body2" color="text.secondary">{new Date(offer.mealDate).toLocaleString()}</Typography><Typography variant="caption" color="text.secondary">Claim before {new Date(offer.expiryTime).toLocaleString()}</Typography><AppButton size="small" sx={{ mt: 1 }} onClick={() => claim(offer._id)}>Claim meal</AppButton></Box>) : <Typography color="text.secondary">There are no meals available to claim right now.</Typography>}</Stack></AppCard></Grid><Grid size={{ xs: 12, lg: 6 }}><AppCard><Typography variant="h6" fontWeight={800} mb={2}>Your recovery activity</Typography><Typography variant="subtitle2" fontWeight={750}>Offered meals</Typography><Stack spacing={1} mt={1} mb={2}>{offered.length ? offered.map((offer) => <Typography key={offer._id} variant="body2">{mealLabel(offer)} · {offer.status}</Typography>) : <Typography variant="body2" color="text.secondary">No meals offered yet.</Typography>}</Stack><Typography variant="subtitle2" fontWeight={750}>Claimed meals</Typography><Stack spacing={1} mt={1}>{claimed.length ? claimed.map((offer) => <Typography key={offer._id} variant="body2">{mealLabel(offer)} · claimed {new Date(offer.claimedTime).toLocaleString()}</Typography>) : <Typography variant="body2" color="text.secondary">No meals claimed yet.</Typography>}</Stack></AppCard></Grid></Grid></>; }
export default MealRecoveryPage;
