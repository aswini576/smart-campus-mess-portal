import { useEffect, useState } from 'react'; import { Grid, Typography } from '@mui/material';
import GroupsRoundedIcon from '@mui/icons-material/GroupsRounded'; import AppCard from '../components/AppCard'; import AppTable from '../components/AppTable'; import LoadingState from '../components/LoadingState'; import ErrorState from '../components/ErrorState'; import { MetricCard, PageHeading } from './DashboardComponents'; import { getAttendanceStats } from '../services/adminService';
function AdminDashboard() { const [stats, setStats] = useState(null); const [error, setError] = useState(''); useEffect(() => { getAttendanceStats().then(setStats).catch((e) => setError(e.response?.data?.message || 'Unable to load administration data.')); }, []); if (!stats && !error) return <LoadingState message="Loading administration dashboard…" />; if (error) return <ErrorState message={error} />; const orderTotal = stats.totals.reduce((sum, item) => sum + item.count, 0); const rows = stats.totals.map((item) => ({ id: item._id, role: item._id, count: item.count, active: item._id === 'cancelled' ? '—' : item.count, status: item._id === 'cancelled' ? 'Cancelled' : 'Active' })); return <><PageHeading title="Campus administration" subtitle="Monitor users, attendance, and mess operations." /><Grid container spacing={2.5}><Grid size={{ xs: 12, sm: 4 }}><MetricCard title="Total users" value={stats.totalUsers} note={`${stats.students} student and staff accounts`} /></Grid><Grid size={{ xs: 12, sm: 4 }}><MetricCard title="Meal bookings" value={orderTotal} note="All-time attendance records" color="#e53935" /></Grid><Grid size={{ xs: 12, sm: 4 }}><MetricCard title="Meal categories" value={stats.byMeal.length} note="With confirmed bookings" color="#ff8f00" /></Grid><Grid size={{ xs: 12, lg: 7 }}><AppCard><Typography variant="h6" fontWeight={800} mb={2}>Attendance overview</Typography><AppTable columns={[{ key: 'role', label: 'Booking status' }, { key: 'count', label: 'Records' }, { key: 'active', label: 'Current' }, { key: 'status', label: 'State' }]} rows={rows} /></AppCard></Grid><Grid size={{ xs: 12, lg: 5 }}><AppCard sx={{ height: '100%', display: 'grid', placeItems: 'center', textAlign: 'center' }}><GroupsRoundedIcon color="primary" sx={{ fontSize: 48 }} /><Typography fontWeight={800} mt={1}>Campus management</Typography><Typography variant="body2" color="text.secondary">Use the navigation to manage students and staff, mess chiefs, reports, and system settings.</Typography></AppCard></Grid></Grid></>; }
export default AdminDashboard;
